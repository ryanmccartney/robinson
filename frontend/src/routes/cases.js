import React from "react";
import Typography from "@mui/material/Typography";

const Cases = () => {
    return (
        <>
            <Typography variant="body1">Cases Page</Typography>
        </>
    );
};

export default Cases;
